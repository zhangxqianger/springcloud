spring:
  application:
    name: config-client
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18000/eureka

server:
  port: 9004